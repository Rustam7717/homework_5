package com.company;

public class Color {
    String outside;
    String inside;

    public Color(String outside, String inside) {
        this.outside = outside;
        this.inside = inside;

    }
}
